# 棋理 — 中国象棋 AI 教练实施计划

## 产品目标

帮助中国象棋初中级玩家理解“为什么”，而不只是显示最佳着法和局面分数。

首个 Web 版本包含两个入口：

1. 人机对弈：选择电脑水平，可随时开启或关闭实时分析。
2. 导入复盘：粘贴棋谱、上传棋谱或手动摆盘，自动找出关键转折点。

## 核心原则

- Pikafish 负责计算，不让大模型自行判断最佳着法。
- 规则层负责验证将军、吃子、牵制、无根子、攻击与防守等事实。
- AI 只把结构化事实转换为自然、易懂的中文解释。
- 每个解释必须能够指回具体棋盘事实或引擎变化。
- 第一版优先解决战术和直接错误，不承诺覆盖所有复杂战略棋理。

## 当前已完成：原型阶段

- 可操作的标准中国象棋棋盘。
- 车、马、炮、象、士、将、兵的基础合法走法。
- 将帅照面和走后被将规则过滤。
- 用户执红与电脑对弈。
- 入门、进阶、高手三个原型难度。
- 着法历史、悔棋、重新开始、翻转棋盘。
- 实时分析开关。
- 基于棋盘事实的原型解释：吃子、将军、出子、中心控制、落点受攻击。
- 最佳候选着和训练原则展示。

## Phase 1：可用 MVP

### 1. Pikafish 服务

建立独立引擎服务：

- 输入：FEN / moves / side / depth / multipv。
- 输出：候选着、分数、主要变化、搜索深度。
- 设定分析超时和并发队列。
- 缓存相同局面分析结果。
- 记录 Pikafish 版本和配置，保证结果可重现。

建议接口：

- `POST /api/engine/analyze-position`
- `POST /api/engine/analyze-game`
- `GET /api/engine/health`

### 2. 正式中国象棋规则库

当前规则代码是自研原型。MVP 需要补充：

- 完整将军、应将、困毙和胜负判断测试。
- 重复局面、长将、长捉等中国象棋规则。
- 标准 FEN 与坐标转换。
- 标准中文记谱，处理同一路多个相同棋子。
- 单元测试覆盖常见边界局面。

### 3. 棋盘事实提取器

对“用户着”和“引擎最佳着”分别提取：

- 是否将军、吃子或形成杀棋威胁。
- 哪些棋子新受攻击、失去保护或增加保护。
- 是否形成双重攻击、牵制、串击、闪击。
- 是否开放或封闭车线、炮架和马腿。
- 是否交换关键进攻子或防守子。
- 主要变化中对手的最强惩罚。

所有事实输出结构化 JSON，并附验证依据。

### 4. AI 教练解释层

输入只包含：

- 当前局面。
- 用户走法。
- Pikafish 候选着、分数和主要变化。
- 事实提取器的验证结果。
- 用户棋力和解释深度偏好。

输出固定包含：

1. 这一步的意图。
2. 这一步具体的问题。
3. 对手最强的惩罚方式。
4. 最佳着解决了什么。
5. 下一次可以复用的棋理。

解释生成后进行二次校验；未通过验证的棋盘事实不得展示。

### 5. 棋谱导入与复盘

首批支持：

- 粘贴标准中文棋谱。
- 粘贴坐标走法。
- 上传 PGN/TXT。
- 手动摆盘并指定行棋方。

分析流程：

1. 对全局快速扫描。
2. 找出分数变化最大的 3–5 个转折点。
3. 对关键局面进行更深搜索。
4. 生成复盘摘要和针对性训练建议。

## Phase 2：学习系统

- 自动识别用户反复出现的错误类型。
- 建立个人弱点标签：漏算、送子、开局重复走子、王区安全、残局转换等。
- 从用户实战局面生成训练题。
- 解释深度分为简短、详细和教练模式。
- 保存对局、复盘和学习进度。

## Phase 3：产品化

- 用户账户和云端棋谱。
- 订阅与使用额度。
- 分享复盘链接。
- 移动端适配。
- 引擎服务扩缩容、成本监控和滥用限制。

## 技术架构建议

### Web

- React + TypeScript。
- Vite 或 Next.js。
- 棋盘与规则逻辑独立为 `xiangqi-core` 包。
- 状态管理保持轻量，优先使用 React 状态或 Zustand。

### API

- Node.js/TypeScript 或 Python FastAPI。
- Pikafish 独立进程池。
- Redis 缓存分析结果和任务状态。
- PostgreSQL 保存用户、棋局、分析和学习画像。

### 部署

- Web 部署在 Vercel。
- Pikafish 服务部署在支持常驻进程和 CPU 计算的服务器，不放在 Vercel Functions。
- AI 解释接口与引擎结果分离，便于替换模型和控制成本。

## 验收标准

MVP 不以“引擎够强”作为唯一标准，而以以下结果验收：

- 用户可以完成一盘合法人机对局。
- 可以导入一盘真实棋局。
- 系统能稳定找出主要转折点。
- 每个关键错误至少给出一个可验证原因。
- 推荐走法与 Pikafish 输出一致。
- 解释中不存在不合法走法或虚构威胁。
- 用户读完后能够回答“我错在哪里，下次应该检查什么”。
